version: 2.1
orbs:
  architect: giantswarm/architect@9.1.0

workflows:
  build:
    jobs:
    - architect/go-build:
        name: go-build-mcp-prometheus
        binary: mcp-prometheus
        context: architect
        architectures: "linux/amd64,linux/arm64,darwin/amd64,darwin/arm64,windows/amd64,windows/arm64"
        filters:
          tags:
            only: /^v.*/

    # Branch builds: linux/amd64+arm64 image to gsoci for PR validation.
    - architect/push-to-registries:
        context: architect
        name: push-to-registries
        platforms: "linux/amd64,linux/arm64"
        requires:
        - go-build-mcp-prometheus
        filters:
          branches:
            ignore:
            - main

    # Tag builds: push multi-arch image to gsoci + gsociprivate. Aliyun is
    # handled by the sync-china-registry job below (orb's split-china-push
    # mechanism), so the buildx push no longer crosses the Pacific.
    - architect/push-to-registries:
        context: architect
        name: push-to-registries-release
        platforms: "linux/amd64,linux/arm64"
        split-china-push: true
        requires:
        - go-build-mcp-prometheus
        filters:
          tags:
            only: /^v.*/
          branches:
            ignore: /.*/

    # Mirror gsoci -> Aliyun via the in-China giantswarm/galaxy-runner. Runs
    # in parallel with the chart catalog push; Aliyun mirror failures do NOT
    # block the chart publish.
    - architect/sync-china-registry:
        context: architect
        name: sync-china-registry
        requires:
        - push-to-registries-release
        filters:
          tags:
            only: /^v.*/
          branches:
            ignore: /.*/

    - architect/upload-release-assets:
        context: architect
        name: upload-release-assets
        binary: mcp-prometheus
        requires:
        - go-build-mcp-prometheus
        filters:
          tags:
            only: /^v.*/
          branches:
            ignore: /.*/

    - architect/push-to-app-catalog:
        executor: app-build-suite
        context: architect
        name: build-mcp-prometheus-chart
        app_catalog: giantswarm-catalog
        app_catalog_test: giantswarm-test-catalog
        chart: mcp-prometheus
        push_to_appcatalog: false
        push_to_oci_registry: false
        persist_chart_archive: true
        requires:
        - go-build-mcp-prometheus
        filters:
          tags:
            only: /^v.*/
          branches:
            ignore:
            - main

    - architect/run-tests-with-ats:
        name: execute chart tests
        requires:
        - build-mcp-prometheus-chart
        filters:
          tags:
            only: /^v.*/
          branches:
            ignore:
            - main

    # Branch: push chart after tests + image.
    - architect/push-to-app-catalog:
        executor: app-build-suite
        context: architect
        name: push-mcp-prometheus-to-giantswarm-app-catalog
        app_catalog: giantswarm-catalog
        app_catalog_test: giantswarm-test-catalog
        chart: mcp-prometheus
        requires:
        - execute chart tests
        - push-to-registries
        filters:
          branches:
            ignore:
            - main

    # Tag: push chart after tests + the gsoci image. Intentionally does NOT
    # depend on sync-china-registry so the chart catalog can publish even if
    # the in-China Aliyun mirror is slow or fails.
    - architect/push-to-app-catalog:
        executor: app-build-suite
        context: architect
        name: push-mcp-prometheus-to-giantswarm-app-catalog-release
        app_catalog: giantswarm-catalog
        app_catalog_test: giantswarm-test-catalog
        chart: mcp-prometheus
        requires:
        - execute chart tests
        - push-to-registries-release
        filters:
          tags:
            only: /^v.*/
          branches:
            ignore: /.*/
